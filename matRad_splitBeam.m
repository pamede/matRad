function newStf = matRad_splitBeam(stf, machine, N, sigmaSub)

newRay = stf.ray;
sigmaRay = stf.ray.energy / 10;
[weights, xPos, yPos] = matRad_calcWeights(sigma, 5, 3)

newStf = stf
for i = 1:size(weights,1)
    

    sigma =10,N=21,sigmaSub=3;
tic
toc



end
